
services:
  dev:
    build: .
    environment:
      - WATCHER_RESTART_DELAY=1.0
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
      - ./dist:/app/dist
  
  test:
    build: .
    command: ["python", "-m", "unittest", "discover", "tests"]
    volumes:
      - .:/app
